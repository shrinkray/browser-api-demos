
console.log(navigator.userAgent)

/**
 * This was a test to see if the navigator object was available in the browser.
 * What we see is that whatever the userAgent displays it's not very helpful.
 * We even see that the Device that is shown is wrong; it's showing an Intel machine when I'm driving an M1 today.
 */